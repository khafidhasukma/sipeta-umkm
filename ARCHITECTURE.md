# SIPETA-UMKM - Arsitektur Sistem

## 🏗️ Diagram Arsitektur

```
┌─────────────────────────────────────────────────────────────────────┐
│                         SIPETA-UMKM SYSTEM                          │
│                    Laravel 12 Monolith Architecture                 │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐          ┌──────────────────────────┐
│     Admin Panel (Pemkot) │          │    UMKM Panel (Users)    │
│      Route: /admin       │          │      Route: /umkm        │
└────────────┬─────────────┘          └────────────┬─────────────┘
             │                                     │
             │                                     │
             ▼                                     ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    FilamentPHP v4 Layer                             │
│  ┌──────────────────────┐          ┌──────────────────────┐         │
│  │  AdminPanelProvider  │          │  UmkmPanelProvider   │         │
│  ├──────────────────────┤          ├──────────────────────┤         │
│  │ • Resources          │          │ • Resources          │         │
│  │ • Pages              │          │ • Pages              │         │
│  │ • Widgets            │          │ • Widgets            │         │
│  │ • Forms              │          │ • Forms              │         │
│  │ • Tables             │          │ • Tables             │         │
│  └──────────────────────┘          └──────────────────────┘         │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Laravel 12 Application Core                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                     Eloquent ORM Layer                        │  │
│  │                                                                │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │  │
│  │  │ BaseModel    │  │ User Model   │  │ UMKM Model   │        │  │
│  │  │ (HasUuids)   │  │ (HasUuids)   │  │ (extends     │  ...   │  │
│  │  │              │  │              │  │  BaseModel)  │        │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘        │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Database Layer (MySQL)                         │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │ users        │  │ umkms        │  │ produks      │             │
│  │──────────────│  │──────────────│  │──────────────│             │
│  │ id (UUID)    │  │ id (UUID)    │  │ id (UUID)    │             │
│  │ name         │  │ user_id (FK) │  │ umkm_id (FK) │   ...       │
│  │ email        │  │ nama_umkm    │  │ nama_produk  │             │
│  │ password     │  │ alamat       │  │ harga        │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
│                                                                     │
│  * Semua tabel menggunakan UUID sebagai Primary Key                │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### Admin Panel Request Flow

```
User Browser
    │
    ├─→ GET /admin
    │
    ▼
AdminPanelProvider
    │
    ├─→ Middleware Stack
    │   ├─ EncryptCookies
    │   ├─ StartSession
    │   ├─ VerifyCsrfToken
    │   ├─ Authenticate
    │   └─ ...
    │
    ▼
Filament Resources
    │
    ├─→ Controllers/Actions
    │
    ▼
Eloquent Models (BaseModel with UUID)
    │
    ├─→ Database Query
    │
    ▼
MySQL Database (UUID Primary Keys)
    │
    ├─→ Return Data
    │
    ▼
Filament Tables/Forms
    │
    ├─→ Render Livewire Components
    │
    ▼
Blade Views + Tailwind CSS v4
    │
    └─→ Response to Browser
```

### UMKM Panel Request Flow

```
UMKM User Browser
    │
    ├─→ GET /umkm
    │
    ▼
UmkmPanelProvider
    │
    ├─→ Middleware Stack
    │   ├─ EncryptCookies
    │   ├─ StartSession
    │   ├─ VerifyCsrfToken
    │   ├─ Authenticate
    │   └─ ...
    │
    ▼
Filament Umkm Resources
    │
    ├─→ Scoped Queries (User's own data)
    │
    ▼
Eloquent Models
    │
    ├─→ WHERE user_id = current_user_id
    │
    ▼
Database (Filtered by User)
    │
    └─→ Response to Browser
```

---

## 📁 Directory Structure Detail

```
sipeta-umkm/
│
├── app/
│   ├── Models/
│   │   ├── BaseModel.php              ← UUID template
│   │   ├── User.php                   ← Authentication
│   │   ├── Umkm.php                   ← UMKM entity (to be created)
│   │   └── Produk.php                 ← Product entity (to be created)
│   │
│   ├── Filament/                      ← ADMIN PANEL
│   │   ├── Resources/
│   │   │   ├── UmkmResource.php
│   │   │   └── ProdukResource.php
│   │   ├── Pages/
│   │   │   └── Dashboard.php
│   │   └── Widgets/
│   │       └── StatsOverview.php
│   │
│   ├── Filament/Umkm/                 ← UMKM PANEL
│   │   ├── Resources/
│   │   │   └── ProdukResource.php
│   │   ├── Pages/
│   │   │   └── Dashboard.php
│   │   └── Widgets/
│   │       └── MyStats.php
│   │
│   └── Providers/
│       ├── AppServiceProvider.php
│       └── Filament/
│           ├── AdminPanelProvider.php
│           └── UmkmPanelProvider.php
│
├── database/
│   ├── migrations/
│   │   ├── 0001_01_01_000000_create_users_table.php      ← UUID
│   │   ├── xxxx_create_umkms_table.php                   ← UUID (to create)
│   │   └── xxxx_create_produks_table.php                 ← UUID (to create)
│   │
│   ├── factories/
│   │   ├── UserFactory.php
│   │   ├── UmkmFactory.php                               ← to create
│   │   └── ProdukFactory.php                             ← to create
│   │
│   └── seeders/
│       ├── DatabaseSeeder.php
│       ├── AdminUserSeeder.php                           ← ✅ Created
│       ├── UmkmSeeder.php                                ← to create
│       └── ProdukSeeder.php                              ← to create
│
├── resources/
│   ├── css/
│   │   └── app.css                    ← Tailwind v4 config
│   ├── js/
│   │   ├── app.js
│   │   └── bootstrap.js
│   └── views/
│       └── welcome.blade.php
│
├── routes/
│   ├── web.php                        ← Public routes
│   └── console.php                    ← Artisan commands
│
├── tests/
│   ├── Feature/
│   │   ├── ExampleTest.php
│   │   ├── UmkmTest.php               ← to create
│   │   └── ProdukTest.php             ← to create
│   └── Unit/
│       └── ExampleTest.php
│
└── Documentation/
    ├── README.md                      ← ✅ Main readme
    ├── SETUP-GUIDE.md                 ← ✅ Setup guide
    ├── UUID-REFERENCE.md              ← ✅ UUID templates
    ├── COMMANDS.md                    ← ✅ Commands ref
    ├── SETUP-COMPLETED.md             ← ✅ Completion summary
    └── ARCHITECTURE.md                ← ✅ This file
```

---

## 🔐 Authentication & Authorization Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      User Authentication                        │
└─────────────────────────────────────────────────────────────────┘

1. LOGIN REQUEST
   ├─ User → /admin/login atau /umkm/login
   ├─ Filament Login Page
   ├─ Validate credentials
   └─ Create session

2. AUTHENTICATED SESSION
   ├─ Session stored in database (sessions table)
   ├─ user_id (UUID) linked to users table
   └─ Remember token for "Remember Me"

3. AUTHORIZATION (Future)
   ├─ Role-based access control
   ├─ Permission system
   │  ├─ Admin: Full access
   │  └─ UMKM: Own data only
   └─ Policy enforcement

4. LOGOUT
   ├─ Destroy session
   └─ Redirect to login
```

---

## 🎯 Data Access Pattern

### Admin Panel (Full Access)

```php
// Admin dapat melihat SEMUA data
Umkm::all();
Produk::with('umkm')->get();
```

### UMKM Panel (Scoped Access)

```php
// UMKM hanya melihat data milik mereka
auth()->user()->umkm;
auth()->user()->umkm->produks;

// Atau dengan global scope
Produk::where('user_id', auth()->id())->get();
```

---

## 🗄️ Database Relationship Diagram

```
┌──────────────────────┐
│       users          │
│──────────────────────│
│ id (UUID) PK         │◄──────────┐
│ name                 │           │
│ email                │           │
│ password             │           │
│ created_at           │           │
│ updated_at           │           │
└──────────────────────┘           │
                                   │ 1:1 or 1:M
                                   │
┌──────────────────────┐           │
│       umkms          │           │
│──────────────────────│           │
│ id (UUID) PK         │           │
│ user_id (UUID) FK    │───────────┘
│ nama_umkm            │
│ alamat               │
│ no_telp              │◄──────────┐
│ created_at           │           │
│ updated_at           │           │
└──────────────────────┘           │ 1:M
                                   │
┌──────────────────────┐           │
│      produks         │           │
│──────────────────────│           │
│ id (UUID) PK         │           │
│ umkm_id (UUID) FK    │───────────┘
│ nama_produk          │
│ deskripsi            │
│ harga                │
│ stok                 │
│ created_at           │
│ updated_at           │
└──────────────────────┘

Many-to-Many Example (Future):

┌──────────────┐      ┌─────────────────┐      ┌──────────────┐
│   umkms      │      │  kategori_umkm  │      │  kategoris   │
│──────────────│      │─────────────────│      │──────────────│
│ id (UUID) PK │◄─────│ umkm_id (UUID)  │      │ id (UUID) PK │
└──────────────┘      │ kategori_id     │─────►│ nama         │
                      │ created_at      │      └──────────────┘
                      └─────────────────┘
                      Composite PK: (umkm_id, kategori_id)
```

---

## 🔧 Technology Stack Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                        Frontend Layer                           │
│  • Blade Templates (Laravel)                                    │
│  • Livewire v3 (Reactive Components)                            │
│  • Tailwind CSS v4 (Utility-first CSS)                          │
│  • Alpine.js (Lightweight JS framework - bundled with Livewire) │
│  • Vite (Asset bundler)                                         │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      Admin Interface Layer                      │
│  • FilamentPHP v4 (Admin Panel Builder)                         │
│  • Filament Forms (Dynamic form builder)                        │
│  • Filament Tables (Advanced tables)                            │
│  • Filament Notifications (Toast notifications)                 │
│  • Filament Widgets (Dashboard widgets)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      Application Layer                          │
│  • Laravel 12 (PHP Framework)                                   │
│  • Controllers (HTTP Request handling)                          │
│  • Middleware (Request filtering)                               │
│  • Validation (Form request validation)                         │
│  • Authorization (Gates & Policies)                             │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                       Business Logic Layer                      │
│  • Eloquent Models (ORM)                                        │
│  • Relationships (HasMany, BelongsTo, etc.)                     │
│  • Scopes (Query scopes)                                        │
│  • Mutators & Accessors (Data formatting)                       │
│  • Events & Listeners (Business events)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                       Database Layer                            │
│  • MySQL / PostgreSQL                                           │
│  • UUID Primary Keys                                            │
│  • Migrations (Schema versioning)                               │
│  • Seeders (Data seeding)                                       │
│  • Indexes & Foreign Keys                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture (Future)

```
┌──────────────────────────────────────────────────────────────────┐
│                         Production Server                        │
│                                                                  │
│  ┌────────────────┐        ┌─────────────────┐                  │
│  │  Nginx/Apache  │───────►│  PHP-FPM 8.2+   │                  │
│  │  (Web Server)  │        │  (Application)   │                  │
│  └────────────────┘        └─────────────────┘                  │
│          │                          │                            │
│          │                          ▼                            │
│          │                 ┌─────────────────┐                  │
│          │                 │  Laravel Queue  │                  │
│          │                 │  Worker         │                  │
│          │                 └─────────────────┘                  │
│          │                                                       │
│          ▼                                                       │
│  ┌────────────────┐                                             │
│  │  Static Assets │                                             │
│  │  (CSS, JS)     │                                             │
│  └────────────────┘                                             │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                       MySQL Database Server                      │
│  • Master-Slave replication (optional)                           │
│  • Automated backups                                             │
│  • UUID indexed tables                                           │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                       Cache Layer (Optional)                     │
│  • Redis / Memcached                                             │
│  • Session storage                                               │
│  • Cache storage                                                 │
│  • Queue storage                                                 │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📊 Performance Considerations

### Database Optimization

-   ✅ UUID indexed for fast lookups
-   ✅ Foreign keys with cascading deletes
-   🔜 Eager loading to prevent N+1 queries
-   🔜 Query caching for frequently accessed data

### Frontend Optimization

-   ✅ Vite for fast asset bundling
-   ✅ Tailwind CSS purging unused styles
-   🔜 Lazy loading for images
-   🔜 Component caching

### Backend Optimization

-   ✅ Eloquent ORM for efficient queries
-   🔜 Queue jobs for heavy tasks
-   🔜 Cache layer (Redis)
-   🔜 API rate limiting

---

## 🔄 Development Workflow

```
1. CREATE FEATURE BRANCH
   ├─ git checkout -b feature/nama-fitur

2. DEVELOP
   ├─ Create Model (extend BaseModel)
   ├─ Create Migration (use UUID)
   ├─ Create Factory & Seeder
   ├─ Create Filament Resources
   └─ Write Tests

3. TEST
   ├─ php artisan test
   ├─ vendor/bin/pint (format)
   └─ Manual testing

4. COMMIT
   ├─ git add .
   ├─ git commit -m "Add: Deskripsi"
   └─ git push origin feature/nama-fitur

5. DEPLOY (Future)
   ├─ Merge to main
   ├─ Run CI/CD pipeline
   └─ Deploy to production
```

---

**Created**: 17 Desember 2025  
**Version**: 1.0.0  
**Status**: Development Ready ✅
